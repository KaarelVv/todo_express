<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Compiled and minified CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <title>Todo List</title>
</head>

<body>
    <div class="container">
        <div class="row">
            <div class="col s12">
                <div class="card">
                    <div class="card-content">
                        <span class="card-title">ToDo List</span>
                        <div class="row">
                            <form class="col s12" method="post" action="update">
                                <div class="row">
                                    <div class="input-field col s12">
                                        <input placeholder="Task" id="task" type="text" name="task" class="validate">
                                        <label for="task" class="active">Update Task</label>
                                        <input id="taskId" type="hidden" name="task" value="6">
                                        </div>
                                </div>
                                <input class="btn" type="submit" value="Update Task">
                                <input class="btn" type="submit" value="Cancel">

                            </form>
                        </div>
                    </div>
                    <!-- <div class="card-action">
                        <ul class="collection">
                            <% console.log(tasks)%>
                                <% tasks.forEach((task)=> { %>
                                    <li class="collection-item">
                                        <%= task.task %>
                                            
                                            <a href="update-task/<%= task.id %>" class="secondary-content">
                                                <i class="material-icons">edit</i></a>
                                    </li>
                                    <% }) %>
                        </ul>
                        <a href="delete-all-tasks/"><input class="btn" type="submit" value="Clear All"></a>
                    </div> -->
                </div>
            </div>
        </div>
    </div>

    <!-- Compiled and minified CSS -->
    <!-- <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css"> -->
    <!-- <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css"></script> -->
</body>

</html>